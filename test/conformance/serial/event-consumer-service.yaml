apiVersion: v1
kind: Service
metadata:
  name: ptp-event-consumer-service
  namespace: openshift-ptp
  labels:
    app: ptp-event-consumer
spec:
  selector:
    app: ptp-event-consumer
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
  type: ClusterIP 